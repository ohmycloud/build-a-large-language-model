[project]
name = "build-a-large-language-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tiktoken>=0.8.0",
]

[tool.uv.workspace]
members = ["ch02-working-with-text-data"]
